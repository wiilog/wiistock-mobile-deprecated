export interface TransportRoundLine {
    order_id: number,
    contact_name: string,
    contact_address: string,
    request_type: string,
    priority: number,
    estimated_at: string,
    expected_at: string,
}