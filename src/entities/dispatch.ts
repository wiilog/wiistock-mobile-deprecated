export interface Dispatch {
    id: number;
    requester: string;
    number: string;
    startDate: string;
    endDate: string;
    urgent: number;
    locationFromLabel: string;
    locationToLabel: string;
    typeLabel: string;
    statusLabel: string;
}
