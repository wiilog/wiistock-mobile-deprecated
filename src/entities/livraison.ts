export interface Livraison {
    id: number;
    number: string;
    preparationLocation: string;
    location: string;
    date_end: string;
    requester: string;
    type: string;
    comment: string
    expectedAt: string
    color: string
    project: string
}
