export class ArticleMouvement {
    id_article: number;
    id_mouvement: number;
}

export class Prise {
    id: number;
    article_id: number;
    quantite: number;
    date_prise
}


`id` INTEGER PRIMARY KEY AUTOINCREMENT, `article_id` INTEGER, `quantite` INTEGER, `date_prise` VARCHAR(255), `username_prise` VARCHAR(255), `id_emplacement_prise` INTEGER)', [])
